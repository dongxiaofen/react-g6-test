import React, { Component } from 'react';
import { observer } from 'mobx-react';

@observer
export default class UserAgreement extends Component {
  render() {
    return (
      <div>
       this is UserAgreement
      </div>
    );
  }
}

import React from 'react';
import { observer } from 'mobx-react';
import styles from './index.less';
function ProvinceRank() {
  return (
    <div className={styles.wrapper}>
      ProvinceRank
    </div>
  );
}
export default observer(ProvinceRank);

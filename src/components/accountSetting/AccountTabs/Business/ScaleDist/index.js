import React from 'react';
import { observer } from 'mobx-react';
import styles from './index.less';
function ScaleDist() {
  return (
    <div className={styles.wrapper}>
      ScaleDist
    </div>
  );
}
export default observer(ScaleDist);

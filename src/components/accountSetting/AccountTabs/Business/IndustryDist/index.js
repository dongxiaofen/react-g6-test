import React from 'react';
import { observer } from 'mobx-react';
import styles from './index.less';
function IndustryDist() {
  return (
    <div className={styles.wrapper}>
      IndustryDist
    </div>
  );
}
export default observer(IndustryDist);

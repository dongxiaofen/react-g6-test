import React, {PropTypes} from 'react';
import { observer } from 'mobx-react';

function ChattelMortgage({}) {
  return (
    <div>

    </div>
  );
}

ChattelMortgage.propTypes = {
  foo: PropTypes.string,
};
export default observer(ChattelMortgage);

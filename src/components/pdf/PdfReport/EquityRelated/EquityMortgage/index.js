import React, {PropTypes} from 'react';
import { observer } from 'mobx-react';

function EquityMortgage({}) {
  return (
    <div>

    </div>
  );
}

EquityMortgage.propTypes = {
  foo: PropTypes.string,
};
export default observer(EquityMortgage);

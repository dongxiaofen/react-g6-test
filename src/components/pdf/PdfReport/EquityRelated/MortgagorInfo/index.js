import React, {PropTypes} from 'react';
import { observer } from 'mobx-react';

function MortgagorInfo({}) {
  return (
    <div>

    </div>
  );
}

MortgagorInfo.propTypes = {
  foo: PropTypes.string,
};
export default observer(MortgagorInfo);

import React, {PropTypes} from 'react';
import { observer } from 'mobx-react';

function CorporateMortgage({}) {
  return (
    <div>

    </div>
  );
}

CorporateMortgage.propTypes = {
  foo: PropTypes.string,
};
export default observer(CorporateMortgage);

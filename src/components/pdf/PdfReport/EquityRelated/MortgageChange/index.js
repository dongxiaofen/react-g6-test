import React, {PropTypes} from 'react';
import { observer } from 'mobx-react';

function MortgageChange({}) {
  return (
    <div>

    </div>
  );
}

MortgageChange.propTypes = {
  foo: PropTypes.string,
};
export default observer(MortgageChange);

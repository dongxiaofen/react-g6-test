import React, {PropTypes} from 'react';
import { observer } from 'mobx-react';
// import styles from './index.less';

function AlterAnalysis({}) {
  return (
    <div>

    </div>
  );
}

AlterAnalysis.propTypes = {
  foo: PropTypes.string,
};
export default observer(AlterAnalysis);

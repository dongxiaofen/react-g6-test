import React, {PropTypes} from 'react';
import { observer } from 'mobx-react';

function DishonestInfo({}) {
  return (
    <div>

    </div>
  );
}

DishonestInfo.propTypes = {
  foo: PropTypes.string,
};
export default observer(DishonestInfo);

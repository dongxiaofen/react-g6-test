import React, {PropTypes} from 'react';
import { observer } from 'mobx-react';

function Fyannouncement({}) {
  return (
    <div>

    </div>
  );
}

Fyannouncement.propTypes = {
  foo: PropTypes.string,
};
export default observer(Fyannouncement);
